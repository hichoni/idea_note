# 반짝! 아이디어노트 (idea_note)

> 이 파일은 프로젝트의 맥락 문서입니다. Claude Code는 작업 시 이 문서를 먼저 읽고
> 앱의 목적·구조·규칙을 이해한 뒤 코드를 수정합니다. 사람이 읽어도 프로젝트 전체를
> 파악할 수 있게 작성했습니다.

---

## 1. 한 줄 요약

초등학생이 **부담 없이 떠오른 생각을 기록**하고, **AI와 함께 아이디어로 키우며**,
최종적으로 **전시관에서 공유**하는 웹 앱 프로토타입.

핵심 철학: **생각 → 아이디어 → 발전 → 발명**
"발명하러 오는 앱"이 아니라 "생각 하나 적으러 오는 앱"이 되도록 설계했다.
그래서 UI 카피에서 '발명'이라는 단어는 성장의 마지막 결과 단계와 전시관에서만
의도적으로 쓰고, 평소에는 '생각·아이디어·기록·성장·탐구'로 표현한다.

- 대상 사용자: 초등학생 (학교 발명/창의 교육 수업 활용)
- 톤: 따뜻함, 창의성, Apple + Notion + Duolingo 감성, 밝고 부드러운 스타일

---

## 2. 기술 개요 (중요)

- **단일 파일 앱이다.** 모든 것이 `index.html` 하나에 들어 있다.
  HTML + CSS(`<style>`) + JS(`<script>`)가 한 파일에 인라인되어 있고,
  **빌드 과정이 없다.** 브라우저로 `index.html`을 열면 그대로 동작한다.
- **의존성**: 외부 CDN 2개만 사용
  - Pretendard 폰트 (jsdelivr, 스타일시트)
  - canvas-confetti (cdnjs, 저장 성공 시 색종이 효과)
  - 이 둘은 인터넷이 없으면 폰트/컨페티만 빠지고 앱 기능은 정상 동작한다.
- **아이콘/이미지**: 25종 3D 아이콘을 160px로 축소해 **base64 data URL로 파일에 내장**했다.
  그래서 이미지 파일이 따로 없어도 어디서나 이미지가 보인다. (파일이 큰 이유 — 약 750KB)
- **저장소 상태**: 데이터는 **브라우저 메모리에만** 존재한다. 새로고침하면 초기화된다.
  (localStorage/서버 없음 — 아래 8. 한계 참고)

수정할 때 원칙: **가능하면 index.html 단일 파일 구조를 유지**한다.
분리(별도 .css/.js/이미지 폴더)가 필요하면 사용자에게 먼저 확인한다.

---

## 3. 화면 구조 (5개 스크린 + 반응형)

하나의 페이지 안에 5개 `<section class="screen">`이 있고, JS `goScreen(id)`로 전환한다.

1. **home** — 히어로, 이번 주 성장 현황, 레벨, 오늘의 질문, 자유 기록 입력, 최근 자라는 아이디어
2. **notes** — 내 아이디어 목록(진행상태·진행바·AI질문수), 정렬(최신/오래된/성장순)
3. **ai** — AI 아이디어 도우미(질문 유형 5종), URL 저장·요약(데모)
4. **gallery** — 아이디어 전시관(Padlet 느낌 카드, 좋아요/댓글/카테고리 필터)
5. **my** — 마이페이지(레벨, 연속기록, 뱃지, 카드형 메뉴)

### 반응형 (두 가지 레이아웃)
- **모바일 세로(<900px)**: 풀스크린 단일 컬럼 + 하단 탭바(`.bottomnav`) + 오렌지 FAB
- **PC(≥900px)**: 좌측 고정 사이드바(`.sidebar`) + 넓은 컨테이너,
  홈은 2단 그리드(`.home-grid`), 노트 2열(`#noteList`), 전시관 3~4열(`.gal`)
- 분기점은 CSS `@media(min-width:900px)`. 창 크기만 바꾸면 자동 전환된다.

---

## 4. 코드 지도 (index.html 안에서 찾는 법)

`<script>` 블록은 위에서 아래로 다음 순서다.

- **에셋 시스템**
  - `ASSET` : 25개 아이콘의 base64 data URL 맵 (키: mascot, bulb, notebook, robot, trophy,
    house, sprout, tree, pencil, magnifier, flask, palette, camera, plane, star, heart,
    book, chat, shield, calendar, pot, target, medal, water, globe, +gear)
  - `EMOJI` : 각 키의 이모지 fallback (에셋이 null이면 이모지 사용)
  - `icon(key, cls)` : 키를 받아 `<img>`(에셋 있으면) 또는 이모지 문자열을 반환
  - `setIcon(id, key)` : 특정 요소의 innerHTML을 아이콘으로 채움
  - `BUDDY_SVG` / `mascot()` : 마스코트. 에셋 없으면 인라인 SVG 캐릭터로 대체
  - 아이콘을 바꾸려면 **여기 매핑만 수정**하면 전 화면에 반영된다.
- **데이터**
  - `CATS` : 카테고리 목록(생활/학교/환경/놀이/안전/AI/기타), `catE`, `CAT_ICON`(카테고리→아이콘키)
  - `statusOf(grow)` : 성장도(0~100)를 상태로 변환 → **아이디어(<40) / 성장중(40~99) / 완성(100)**
  - `notes` : 아이디어 배열. 각 항목 `{id, text, cat, grow, ai, date}`
    - `grow` 성장도(0~100), `ai` 받은 AI 질문 수
  - `gallery` : 전시관 카드 배열 `{title, cat, likes, cmt, icon, bg}` (친구들 샘플 + 완성한 내 아이디어)
  - `MENU` : 마이페이지 메뉴, `NAV_ICON` : 하단탭/사이드바 아이콘 매핑
  - `QUESTIONS` : 오늘의 질문 후보, `PH` : 입력창 placeholder 후보
- **동작(로직)**
  - `goScreen(id)` : 화면 전환 (+ 하단탭/사이드바 활성표시, 스크롤 리셋, FAB 표시 제어)
  - 오늘의 질문: `otherQ`(다른 질문), `answerBtn`(입력창으로 이동)
  - 카테고리 선택, placeholder 랜덤 순환
  - `saveBtn` : 새 생각 저장 → 컨페티 + 토스트 + 통계 갱신
  - `updateStats()` : 이번 주 성장(심은 생각/자라는 아이디어/완성한 아이디어) 계산
  - `renderRecent()` / `renderNotes()` : 홈·노트 카드 렌더
  - `growNote(id)` : **카드를 누르면 성장도 +20**. 100 도달 시 컨페티 + 전시관에 자동 등록
  - AI: `fillPicker()`, `QTYPES`(질문 유형 5종), `askAI(i)`(질문 시 해당 아이디어 grow +8, ai +1),
    `qrandom`(랜덤 질문)
  - URL: `summarize` + `guessTopic(url)` (키워드 기반 **가짜 요약** — 데모)
  - 전시관: `FILTERS`, `renderGallery()`, 좋아요 토글
  - `esc()` : XSS 방지용 HTML 이스케이프
- **CSS** : `:root`에 디자인 토큰(색/그림자/반경). 화면 아래쪽 `@media(min-width:900px)`가 PC 레이아웃.

---

## 5. 데이터 모델

```js
// 아이디어(생각) 한 건
{ id: 5, text: '우산 물이 안 흐르는 커버', cat: 'life', grow: 5, ai: 0, date: '7월 24일' }
// cat: life|school|env|play|safe|ai|etc
// grow: 0~100 (성장도). statusOf(grow)로 아이디어/성장중/완성 판정
// ai: 받은 AI 질문 개수

// 전시관 카드
{ title:'...', cat:'play', likes:24, cmt:6, icon:'target', bg:'#FFF0D6' }
```

성장 규칙: 저장 시 grow=5(아이디어) → 카드 클릭마다 +20, AI 질문마다 +8 →
100이 되면 '완성'이 되어 전시관으로 올라간다. 이것이 "생각이 자란다"는 핵심 UX다.

---

## 6. 디자인 토큰 (색상)

| 용도 | 값 |
|---|---|
| Primary | `#FFC83D` |
| Secondary | `#98E63D` |
| Accent | `#FF8A3D` |
| Purple(AI) | `#A97CFF` |
| Background | `#FFF9F3` |
| Card | `#FFFFFF` |
| Text | `#2F2F2F` |
| Sub text | `#7A7A7A` |
| Border | `#EFE8DE` |

폰트: Pretendard. 카드: 둥근 모서리 + 부드러운 그림자 + 넉넉한 여백(미니멀).

---

## 7. 카피라이팅 규칙

- '발명' 단어는 최소화. 평소엔 **생각 · 아이디어 · 기록 · 성장 · 탐구**로 표현.
- 단, AI 기능의 "발명 아이디어로 발전시키기", 성장 마지막 단계(발명), 전시관에서는 사용 OK.
- 문장은 짧고 부담 없이. "한 줄만 적어도 괜찮아요" 같은 톤.
- 초등학생 눈높이. 어려운 용어 금지, 이모지/일러스트로 친근하게.

---

## 8. 현재 한계 (프로토타입) — 다음에 개선할 것

1. **저장이 안 된다.** 데이터가 메모리에만 있어 새로고침하면 초기화됨.
   → localStorage 또는 백엔드(DB) 연동 필요.
2. **AI가 진짜가 아니다.** URL 요약(`guessTopic`)과 질문 응답(`QTYPES`)은
   미리 정한 텍스트를 보여주는 **데모**다. → 실제 AI API 연동 필요.
3. **URL 요약이 링크를 실제로 읽지 않는다.** 키워드로 대충 분기할 뿐.
4. 로그인/사용자 계정 없음(레벨·연속기록은 고정값 표시).
5. 전시관 공유·댓글이 실제 저장/공유되지 않음(로컬 데모).

---

## 9. 다음 작업 아이디어 (로드맵)

- [ ] localStorage로 기록 영구 저장 (가장 먼저 하면 체감이 큼)
- [ ] 실제 AI 연동: URL 요약, 아이디어 발전 질문/코칭
- [ ] 교사용 화면: 우리 반 아이디어 모아보기 / 진행상황 대시보드
- [ ] 로그인 & 사용자별 레벨·연속기록 실제 반영
- [ ] 전시관 실제 공유(좋아요/댓글 저장)
- [ ] 접근성(스크린리더 라벨), 키보드 내비게이션 점검

---

## 10. 개발 워크플로우

- 실행: `index.html`을 브라우저로 열기 (빌드/서버 불필요).
  로컬 서버가 필요하면 `python -m http.server` 후 접속.
- 배포: GitHub Pages (Settings → Pages → main / root). `.nojekyll` 포함되어 있음.
  → `https://hichoni.github.io/idea_note/`
- 커밋 후 `git push`로 반영.
- **수정 시 원칙**: 단일 파일 구조·디자인 토큰·카피 규칙(7번)을 지킬 것.
  큰 구조 변경(파일 분리, 프레임워크 도입 등)은 사용자와 먼저 상의.
